---
name: learned
description: Placeholder skill imported from the source Claude Code config. The original directory was empty in windows-claude-code/claude/skills/learned/. Replace this content when authoring the actual skill.
---

# learned (placeholder)

This skill directory existed in the source CC config as an empty placeholder. A `SKILL.md` was added during the Codex migration audit so the directory loads as a valid Codex skill rather than being silently skipped.

When ready to author the real skill, replace this body and update the description in the frontmatter to match.
